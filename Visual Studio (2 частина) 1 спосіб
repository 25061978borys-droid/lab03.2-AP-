// спосіб 1: розгалуження в скороченій формі
if (x == 0 && b != 0)
	F = a * ((x + c) * (x + c)) - b;
if (x == 0 && b == 0)
	F = (x - a / (-c));
if (!(x == 0 && b != 0) && !(x == 0 && b == 0))
	F = a + (x / c);

cout << endl;
cout << "1) F = " << F << endl;
