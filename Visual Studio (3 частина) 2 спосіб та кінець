	// спосіб 2: розгалуження в повній формі
	if (x == 0 && b != 0)
		F = a * ((x + c) * (x + c)) - b;
	else
		if (x == 0 && b == 0)
			F = (x - a / (-c));
		else
			F = a + (x / c);

	cout << "2) F = " << F << endl;

	cin.get();
	return 0;


}
